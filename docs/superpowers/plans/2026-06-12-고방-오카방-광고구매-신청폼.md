# 고방 오픈카톡방 광고 구매 신청폼 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 고방 오픈카톡방 스폰서 광고를 광고주가 직접 신청 → 검수 통과 시 토스 결제링크 알림톡 자동발송하는 신청폼 + GAS 백엔드 + 노션 허브를 만든다.

**Architecture:** 빌리투어(`billytour_request`) 패턴 복제·단순화. 정적 신청폼(GitHub Pages) → GAS 웹앱(doPost) → 구글시트(`설정`/`신청 내역` 2탭) 저장 + 이미지 Drive 업로드 → 운영자 검수 → onEdit 드롭박스 '발송하기' → SOLAPI 카카오 알림톡(토스 결제링크). 상품·검수기준은 `설정`탭 데이터로 분리해 6/25~ 고방 광고 어드민이 카탈로그로 흡수.

**Tech Stack:** HTML + Vanilla JS, Google Apps Script(clasp), 구글시트, SOLAPI, 토스페이먼츠 결제링크, GitHub Pages, Notion.

---

## File Structure

```
프로젝트/for_Release/고방-광고상품/
├── index.html              신청폼 1장 (상품안내 + 폼 + 검수중 화면)
├── colors_and_type.css     고방 디자인 토큰
├── gas-code/
│   ├── Code.gs             GAS 백엔드 (doPost/submitForm/이미지업로드/onEdit/SOLAPI/setup*)
│   ├── appsscript.json     GAS manifest
│   └── .clasp.json         clasp 바인딩(scriptId) — 커밋 제외(.gitignore)
├── helpers.test.js         순수 헬퍼 단위 테스트 (node --test)
├── helpers.js              순수 헬퍼(상품 라벨·결제링크 매칭) — Code.gs와 index.html 공유 로직 검증용
├── README.md               단일 진실 소스(개요·코어·배포링크)
├── docs/superpowers/
│   ├── specs/2026-06-12-...-design.md   (작성됨)
│   └── plans/2026-06-12-...md           (이 파일)
└── 시작 8080.bat           로컬 폼 미리보기
```

**바인딩 시트**: `18_OqLH7IuTznPL7gmhC-bfFeS9n28hIL6-fQy089Qxc`
**상품/금액**: 게시 1회 `55000` (코드 'A', 라벨 '게시1회') / 게시+공지고정24h `110000` (코드 'B', 라벨 '게시+공지고정')

---

## Task 1: repo 스캐폴드 + 순수 헬퍼 + 테스트

**Files:**
- Create: `프로젝트/for_Release/고방-광고상품/helpers.js`
- Test: `프로젝트/for_Release/고방-광고상품/helpers.test.js`

- [ ] **Step 1: 실패 테스트 작성** — `helpers.test.js`

```js
const { test } = require('node:test');
const assert = require('node:assert');
const { productLabel, payLinkFor, normalizePhone, PRODUCTS } = require('./helpers');

test('productLabel: 코드→라벨', () => {
  assert.strictEqual(productLabel('A'), '게시1회');
  assert.strictEqual(productLabel('B'), '게시+공지고정');
  assert.strictEqual(productLabel('X'), '게시1회'); // 기본값 A
});

test('payLinkFor: 라벨→토스링크', () => {
  const links = { basic: 'https://t/basic', pin: 'https://t/pin' };
  assert.strictEqual(payLinkFor('게시+공지고정', links), 'https://t/pin');
  assert.strictEqual(payLinkFor('게시1회', links), 'https://t/basic');
});

test('normalizePhone: 숫자만', () => {
  assert.strictEqual(normalizePhone('010-1234-5678'), '01012345678');
});

test('PRODUCTS: 금액 정의', () => {
  assert.strictEqual(PRODUCTS.A.amount, 55000);
  assert.strictEqual(PRODUCTS.B.amount, 110000);
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd "프로젝트/for_Release/고방-광고상품" && node --test`
Expected: FAIL — `Cannot find module './helpers'`

- [ ] **Step 3: 최소 구현** — `helpers.js`

```js
const PRODUCTS = {
  A: { label: '게시1회', amount: 55000, key: 'basic' },
  B: { label: '게시+공지고정', amount: 110000, key: 'pin' },
};

function productLabel(code) {
  return (PRODUCTS[code] || PRODUCTS.A).label;
}

function payLinkFor(label, links) {
  return label === '게시+공지고정' ? links.pin : links.basic;
}

function normalizePhone(s) {
  return String(s || '').replace(/[^0-9]/g, '');
}

if (typeof module !== 'undefined') {
  module.exports = { PRODUCTS, productLabel, payLinkFor, normalizePhone };
}
```

- [ ] **Step 4: 통과 확인**

Run: `node --test`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git init && git add helpers.js helpers.test.js && git commit -m "feat: 광고 신청폼 순수 헬퍼 + 테스트"
```

---

## Task 2: GAS 백엔드 — doPost/checkAccess/submitForm (이미지 제외)

**Files:**
- Create: `gas-code/Code.gs`, `gas-code/appsscript.json`

- [ ] **Step 1: `appsscript.json`**

```json
{
  "timeZone": "Asia/Seoul",
  "dependencies": {},
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "webapp": { "executeAs": "USER_DEPLOYING", "access": "ANYONE_ANONYMOUS" }
}
```

- [ ] **Step 2: `Code.gs` — 상수 + 설정 + checkAccess + doPost 골격**

```js
var SPREADSHEET_ID = '18_OqLH7IuTznPL7gmhC-bfFeS9n28hIL6-fQy089Qxc';
var DRIVE_FOLDER_NAME = '고방광고_신청이미지';

// SOLAPI 템플릿 (등록 후 채움) — 토스링크는 버튼에 박힘 (빌리투어 방식)
var TEMPLATE_PAYMENT_BASIC = '';  // 게시1회 결제요청
var TEMPLATE_PAYMENT_PIN   = '';  // 게시+공지고정 결제요청

// 토스 결제링크 (생성 후 채움) — 참고용
var PAY_LINK_BASIC = '';  // 게시1회 55,000
var PAY_LINK_PIN   = '';  // 게시+공지고정 110,000

function getSettings() {
  var s = SpreadsheetApp.openById(SPREADSHEET_ID).getSheetByName('설정');
  var data = s.getRange(1, 1, 4, 2).getValues();
  return {
    active:    String(data[0][1]).trim(),
    startDate: data[1][1] ? new Date(data[1][1]) : null,
    endDate:   data[2][1] ? new Date(data[2][1]) : null,
    notifyEmails: String(data[3][1] || '')
      .split(/[,\s]+/).filter(function(x){ return x; }).join(',')
  };
}

function checkAccess() {
  var cfg = getSettings();
  var now = new Date();
  if (cfg.active !== 'ON') return { ok: false, reason: '현재 신청을 받지 않고 있어요.' };
  if (cfg.startDate) {
    cfg.startDate.setHours(0,0,0,0);
    if (now < cfg.startDate) {
      var diff = Math.ceil((cfg.startDate - now) / 86400000);
      return { ok: false, reason: diff + '일 후 신청 가능합니다.' };
    }
  }
  if (cfg.endDate) {
    cfg.endDate.setHours(23,59,59,999);
    if (now > cfg.endDate) return { ok: false, reason: '신청 기간이 마감되었어요.' };
  }
  return { ok: true };
}

function productLabel(code) { return code === 'B' ? '게시+공지고정' : '게시1회'; }

function doGet(e) {
  return ContentService.createTextOutput(JSON.stringify({ status: 'ok' }))
    .setMimeType(ContentService.MimeType.JSON);
}

function doPost(e) {
  var result;
  try {
    var payload = JSON.parse(e.postData.contents);
    if (payload.action === 'checkAccess') {
      var a = checkAccess();
      result = { ok: a.ok, reason: a.reason || '' };
    } else if (payload.action === 'submitForm') {
      result = submitForm(payload);
    } else {
      result = { error: 'Unknown action: ' + payload.action };
    }
  } catch (err) {
    result = { error: err.toString() };
  }
  return ContentService.createTextOutput(JSON.stringify(result))
    .setMimeType(ContentService.MimeType.JSON);
}
```

- [ ] **Step 3: `Code.gs` — submitForm (이미지 제외, 텍스트 필드만)**

```js
/* 신청 내역(13열):
   A=신청일시, B=결제완료일, C=상호, D=연락처, E=상품선택,
   F=광고문구, G=이미지URL, H=게시희망일, I=사업자정보,
   J=검수메모, K=결제링크발송(발송대기/발송하기/발송완료), L=발송시간, M=게시완료 */
function submitForm(formData) {
  var ss = SpreadsheetApp.openById(SPREADSHEET_ID);
  var sheet = ss.getSheetByName('신청 내역');
  var now = new Date();
  var label = productLabel(formData.product);
  var imageUrl = '';
  if (formData.imageData) imageUrl = saveImageToDrive(formData);   // Task 3에서 구현

  sheet.appendRow([
    now, '', formData.company || '', formData.phone || '', label,
    formData.adText || '', imageUrl, formData.publishDate || '',
    formData.bizInfo || '', '', '발송대기', '', '대기'
  ]);
  sheet.getRange(sheet.getLastRow(), 11).setValue('발송대기');
  sheet.getRange(sheet.getLastRow(), 13).setValue('대기');

  try {
    var to = getSettings().notifyEmails || 'archoit94@neoflat.net';
    MailApp.sendEmail(to, '[고방광고] 새 신청 — ' + (formData.company||'') + ' / ' + label, [
      '신청일시: ' + Utilities.formatDate(now,'Asia/Seoul','yyyy-MM-dd HH:mm'),
      '상호: ' + (formData.company||''),
      '연락처: ' + (formData.phone||''),
      '상품: ' + label,
      '게시희망일: ' + (formData.publishDate||''),
      '광고문구:\n' + (formData.adText||''),
      '이미지: ' + imageUrl,
      '사업자정보: ' + (formData.bizInfo||''),
      '▶ 시트: https://docs.google.com/spreadsheets/d/' + SPREADSHEET_ID
    ].join('\n'));
  } catch (mailErr) {}

  return { success: true };
}
```

- [ ] **Step 4: 수동 검증 (배포 후)** — GAS 편집기에서 `setupSheets()` 실행(Task 5) → `doPost` 테스트는 Task 6 폼 E2E에서. 지금은 구문 오류 없는지 `clasp push` 성공 여부로 확인.

Run: `cd gas-code && clasp push`
Expected: `Pushed N files.` (오류 없음)

- [ ] **Step 5: 커밋**

```bash
git add gas-code/Code.gs gas-code/appsscript.json && git commit -m "feat: GAS doPost/checkAccess/submitForm (이미지 제외)"
```

---

## Task 3: 이미지 1장 → Drive 업로드 + URL 기록

**Files:**
- Modify: `gas-code/Code.gs` (saveImageToDrive 추가)

- [ ] **Step 1: `Code.gs`에 `saveImageToDrive` 추가**

```js
/* base64 dataURL(formData.imageData) → Drive 저장 → 공개 보기 URL 반환
   formData.imageData = "data:image/png;base64,...." , formData.imageName = "광고.png" */
function saveImageToDrive(formData) {
  try {
    var m = String(formData.imageData).match(/^data:([^;]+);base64,(.+)$/);
    if (!m) return '';
    var contentType = m[1];
    var bytes = Utilities.base64Decode(m[2]);
    var safeName = (formData.company || 'ad') + '_' +
      Utilities.formatDate(new Date(),'Asia/Seoul','yyyyMMdd_HHmmss') + '_' +
      (formData.imageName || 'image');
    var blob = Utilities.newBlob(bytes, contentType, safeName);

    var folders = DriveApp.getFoldersByName(DRIVE_FOLDER_NAME);
    var folder = folders.hasNext() ? folders.next() : DriveApp.createFolder(DRIVE_FOLDER_NAME);
    var file = folder.createFile(blob);
    file.setSharing(DriveApp.Access.ANYONE_WITH_LINK, DriveApp.Permission.VIEW);
    return 'https://drive.google.com/uc?id=' + file.getId();
  } catch (err) {
    Logger.log('이미지 저장 실패: ' + err);
    return '';
  }
}
```

- [ ] **Step 2: 수동 검증** — `clasp push` 성공 + (배포 후) 폼에서 이미지 첨부 제출 시 Drive 폴더 `고방광고_신청이미지`에 파일 생성 + G열 URL 기록. (Task 6 E2E에서 확인)

- [ ] **Step 3: 커밋**

```bash
git add gas-code/Code.gs && git commit -m "feat: 광고 이미지 base64→Drive 업로드"
```

---

## Task 4: onEdit 검수→결제링크 알림톡 (SOLAPI)

**Files:**
- Modify: `gas-code/Code.gs` (SOLAPI 인증·발송·onEdit·handlePaymentSend 추가)

- [ ] **Step 1: SOLAPI 인증 + 발송 함수** (빌리투어와 동일)

```js
function getSolapiAuthHeader() {
  var props = PropertiesService.getScriptProperties();
  var date = Utilities.formatDate(new Date(),'Asia/Seoul','yyyy-MM-dd HH:mm:ss');
  var salt = Utilities.getUuid();
  var sig = Utilities.computeHmacSha256Signature(date+salt, props.getProperty('SOLAPI_API_SECRET'))
    .map(function(b){ return ('0'+(b&0xFF).toString(16)).slice(-2); }).join('');
  return 'HMAC-SHA256 apiKey=' + props.getProperty('SOLAPI_API_KEY') +
    ', date=' + date + ', salt=' + salt + ', signature=' + sig;
}

function sendAlimtalk(to, templateId, variables) {
  var pfId = PropertiesService.getScriptProperties().getProperty('SOLAPI_PF_ID');
  var payload = { message: { to: String(to).replace(/[^0-9]/g,''),
    kakaoOptions: { pfId: pfId, templateId: templateId, variables: variables } } };
  var res = UrlFetchApp.fetch('https://api.solapi.com/messages/v4/send', {
    method:'post', contentType:'application/json',
    headers:{ Authorization: getSolapiAuthHeader() },
    payload: JSON.stringify(payload), muteHttpExceptions:true });
  Logger.log('알림톡 응답: ' + res.getContentText());
  return res;
}
```

- [ ] **Step 2: onEdit + handlePaymentSend (신청 내역 K열=11 발송, B열=2 결제완료)**

```js
function onEdit(e) {
  var sheet = e.range.getSheet();
  if (sheet.getName() !== '신청 내역') return;
  var row = e.range.getRow(), col = e.range.getColumn();
  if (row < 2) return;
  if (col === 11) handlePaymentSend(e, sheet, row);
}

function handlePaymentSend(e, sheet, row) {
  if (String(e.range.getValue()).trim() !== '발송하기') return;
  if (sheet.getRange(row, 12).getValue()) { e.range.setValue('발송완료'); return; } // L 발송시간 가드
  var rowData = sheet.getRange(row, 1, 1, 13).getValues()[0];
  var company = String(rowData[2] || '').trim();         // C 상호
  var phone   = String(rowData[3]).replace(/[^0-9]/g,''); // D 연락처
  var label   = String(rowData[4] || '').trim();          // E 상품선택
  var templateId = label === '게시+공지고정' ? TEMPLATE_PAYMENT_PIN : TEMPLATE_PAYMENT_BASIC;
  try {
    sendAlimtalk(phone, templateId, { '#{신청자}': company });
    sheet.getRange(row, 12).setValue(Utilities.formatDate(new Date(),'Asia/Seoul','yyyy-MM-dd HH:mm:ss'));
    sheet.getRange(row, 11).setValue('발송완료');
  } catch (err) {
    Logger.log('결제링크 알림톡 실패: ' + err);
    e.range.setValue('발송대기');
  }
}
```

- [ ] **Step 3: 수동 검증** — `clasp push` 성공. (실발송은 SOLAPI 템플릿·시크릿 등록 후 Task 7 운영자 검증)

- [ ] **Step 4: 커밋**

```bash
git add gas-code/Code.gs && git commit -m "feat: onEdit 검수→결제링크 알림톡(SOLAPI)"
```

---

## Task 5: setupSheets/setupDropdowns/setupTrigger + 설정탭 데이터화

**Files:**
- Modify: `gas-code/Code.gs`

- [ ] **Step 1: setup 함수들 추가**

```js
function ensureSheet(ss, name) { if (!ss.getSheetByName(name)) ss.insertSheet(name); }

function setupSheets() {
  var ss = SpreadsheetApp.openById(SPREADSHEET_ID);
  ensureSheet(ss, '설정');
  ensureSheet(ss, '신청 내역');

  var s = ss.getSheetByName('설정');
  if (String(s.getRange(1,1).getValue()).trim() === '') {
    s.getRange(1,1,4,2).setValues([
      ['활성화(ON/OFF)', 'ON'],
      ['시작일', ''],
      ['종료일', ''],
      ['알림 이메일', 'archoit94@neoflat.net']
    ]);
    // 상품 카탈로그(미래 어드민이 읽는 데이터) — 6행~
    s.getRange(6,1,3,4).setValues([
      ['코드','상품명','금액(VAT포함)','정책'],
      ['A','게시1회',55000,'[광고] 표기 업로드, 다음날 자정까지 유지'],
      ['B','게시+공지고정',110000,'게시 + 공지 고정 24시간']
    ]);
  }

  var apply = ss.getSheetByName('신청 내역');
  if (apply.getLastRow() === 0) {
    apply.appendRow(['신청일시','결제완료일','상호','연락처','상품선택',
      '광고문구','이미지URL','게시희망일','사업자정보',
      '검수메모','결제링크 발송','발송시간','게시완료']);
    apply.getRange(1,1,1,13).setFontWeight('bold').setBackground('#f0f0f0');
  }
  setupDropdowns();
  Logger.log('시트 초기화 완료');
}

function setupDropdowns() {
  var apply = SpreadsheetApp.openById(SPREADSHEET_ID).getSheetByName('신청 내역');
  if (!apply) return;
  // K열(11) 결제링크 발송
  var range = apply.getRange(2,11,1000,1);
  range.clearDataValidations();
  range.setDataValidation(SpreadsheetApp.newDataValidation()
    .requireValueInList(['발송대기','발송하기'], false).build());
  var keep = apply.getConditionalFormatRules().filter(function(r){
    return r.getRanges().every(function(rng){ return rng.getColumn() !== 11; }); });
  apply.setConditionalFormatRules(keep.concat([
    SpreadsheetApp.newConditionalFormatRule().whenTextEqualTo('발송대기')
      .setBackground('#F5F5F5').setFontColor('#9E9E9E').setRanges([range]).build(),
    SpreadsheetApp.newConditionalFormatRule().whenTextEqualTo('발송하기')
      .setBackground('#FFF8E1').setFontColor('#F57F17').setRanges([range]).build(),
    SpreadsheetApp.newConditionalFormatRule().whenTextEqualTo('발송완료')
      .setBackground('#E8F5E9').setFontColor('#388E3C').setRanges([range]).build()
  ]));
  // M열(13) 게시완료
  var pub = apply.getRange(2,13,1000,1);
  pub.clearDataValidations();
  pub.setDataValidation(SpreadsheetApp.newDataValidation()
    .requireValueInList(['대기','완료'], false).build());
}

function setupTrigger() {
  ScriptApp.getProjectTriggers().forEach(function(t){
    if (t.getHandlerFunction() === 'onEdit') ScriptApp.deleteTrigger(t); });
  ScriptApp.newTrigger('onEdit')
    .forSpreadsheet(SpreadsheetApp.openById(SPREADSHEET_ID)).onEdit().create();
}
```

- [ ] **Step 2: 수동 검증** — GAS 편집기에서 `setupSheets()` 1회 실행 → 시트에 `설정`(상품 카탈로그 포함)·`신청 내역` 탭 + 헤더 + K열 드롭박스 생성 확인. `setupTrigger()` 실행 → onEdit 설치형 트리거 등록 확인.

- [ ] **Step 3: 커밋**

```bash
git add gas-code/Code.gs && git commit -m "feat: setupSheets/Dropdowns/Trigger + 설정탭 상품 카탈로그"
```

---

## Task 6: 신청폼 index.html + colors_and_type.css

**Files:**
- Create: `index.html`, `colors_and_type.css`, `시작 8080.bat`

- [ ] **Step 1: `colors_and_type.css` — 고방 디자인 토큰**

```css
:root{
  --gobang-primary:#1B64DA;   /* 고방 블루 */
  --gobang-dark:#0F2E66;
  --gobang-accent:#FFB020;
  --bg:#F7F9FC; --surface:#FFFFFF; --text:#1A2233; --muted:#6B7588;
  --border:#E3E8F0; --radius:14px;
  --font:'Pretendard',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;
}
*{box-sizing:border-box} body{margin:0;background:var(--bg);color:var(--text);font-family:var(--font)}
```

- [ ] **Step 2: `index.html` — 안내 + 폼 + 검수중 화면** (핵심 로직 포함)

```html
<!doctype html><html lang="ko"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>고방 오픈카톡방 광고 신청</title>
<link rel="preconnect" href="https://cdn.jsdelivr.net">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css">
<link rel="stylesheet" href="colors_and_type.css">
<style>
  .wrap{max-width:640px;margin:0 auto;padding:24px 18px 80px}
  .hero{background:var(--gobang-dark);color:#fff;border-radius:var(--radius);padding:28px 24px;margin-bottom:20px}
  .hero h1{margin:0 0 6px;font-size:22px} .hero p{margin:0;color:#C7D4EC;font-size:14px}
  .plans{display:grid;gap:12px;margin:18px 0}
  .plan{border:2px solid var(--border);border-radius:var(--radius);padding:16px;background:var(--surface);cursor:pointer;display:flex;justify-content:space-between;align-items:center}
  .plan.sel{border-color:var(--gobang-primary);box-shadow:0 0 0 3px rgba(27,100,218,.12)}
  .plan .price{font-weight:700;color:var(--gobang-primary)}
  label{display:block;font-size:13px;font-weight:600;margin:14px 0 6px}
  input,textarea{width:100%;padding:12px;border:1px solid var(--border);border-radius:10px;font-family:inherit;font-size:15px}
  textarea{min-height:120px;resize:vertical}
  .req::after{content:' *';color:#E53935}
  .btn{width:100%;padding:15px;border:0;border-radius:12px;background:var(--gobang-primary);color:#fff;font-size:16px;font-weight:700;margin-top:22px;cursor:pointer}
  .btn:disabled{opacity:.5}
  .note{font-size:12px;color:var(--muted);margin-top:6px;line-height:1.6}
  .done{text-align:center;padding:60px 20px;display:none}
  .done.show{display:block} .form.hide{display:none}
  .rules{background:#FFF6E5;border-radius:10px;padding:12px;font-size:12px;color:#8a6d3b;margin-top:8px}
</style></head><body>
<div class="wrap">
  <div class="hero">
    <h1>고방 오픈카톡방 광고 신청</h1>
    <p>청년 주거·주택·자금 지원 정보공유 (1,178명) · 스폰서 게시</p>
  </div>

  <form id="form" class="form">
    <div class="plans">
      <div class="plan sel" data-code="A">
        <div><strong>게시 1회</strong><div class="note">[광고] 표기 업로드 · 다음날 자정까지 유지</div></div>
        <div class="price">55,000원</div>
      </div>
      <div class="plan" data-code="B">
        <div><strong>게시 1회 + 공지 고정 24h</strong><div class="note">공지 상단 고정 24시간</div></div>
        <div class="price">110,000원</div>
      </div>
    </div>

    <label class="req">상호 / 광고주명</label>
    <input id="company" required>
    <label class="req">연락처 (휴대폰)</label>
    <input id="phone" type="tel" placeholder="010-0000-0000" required>
    <label class="req">광고 문구</label>
    <textarea id="adText" required placeholder="게시할 광고 문구를 입력해주세요. ([광고] 표기로 업로드됩니다)"></textarea>
    <label class="req">광고 이미지 (1장)</label>
    <input id="image" type="file" accept="image/png,image/jpeg" required>
    <div class="note">jpg/png, 5MB 이하 1장</div>
    <label class="req">게시 희망일</label>
    <input id="publishDate" type="date" required>
    <label>사업자 정보 (세금계산서용 · 선택)</label>
    <input id="bizInfo" placeholder="상호 / 사업자번호 / 이메일">
    <div class="rules">
      ⚠️ 방 규칙: 집광고·어플·유튜브·블로그·카페 등 외부 링크 언급 게시물은 게시 불가합니다. 문구·이미지는 사전 검수 후 [광고] 표기로 업로드됩니다.
    </div>
    <label style="display:flex;gap:8px;align-items:center;font-weight:400;margin-top:12px">
      <input type="checkbox" id="agree" style="width:auto" required> 위 방 규칙에 동의합니다.
    </label>
    <button class="btn" id="submit" type="submit">검수 신청하기</button>
    <div class="note">신청 후 검수(보통 1영업일) → 통과 시 카카오 알림톡으로 결제링크를 보내드립니다. (VAT 포함 금액)</div>
  </form>

  <div class="done" id="done">
    <h2>✅ 신청이 접수됐어요</h2>
    <p class="note">검수 후 통과되면 입력하신 연락처로 결제링크를 보내드립니다.</p>
  </div>
</div>

<script>
const GAS_URL = ''; // ← clasp deploy 후 /exec URL 기입
let product = 'A';
document.querySelectorAll('.plan').forEach(p => p.addEventListener('click', () => {
  document.querySelectorAll('.plan').forEach(x => x.classList.remove('sel'));
  p.classList.add('sel'); product = p.dataset.code;
}));

function fileToDataURL(file){ return new Promise((res,rej)=>{
  const r=new FileReader(); r.onload=()=>res(r.result); r.onerror=rej; r.readAsDataURL(file); }); }

document.getElementById('form').addEventListener('submit', async (e) => {
  e.preventDefault();
  const btn = document.getElementById('submit');
  const file = document.getElementById('image').files[0];
  if (file && file.size > 5*1024*1024) { alert('이미지는 5MB 이하 1장만 가능합니다.'); return; }
  btn.disabled = true; btn.textContent = '제출 중...';
  try {
    const imageData = file ? await fileToDataURL(file) : '';
    const body = {
      action:'submitForm', product,
      company: company.value.trim(), phone: phone.value.trim(),
      adText: adText.value.trim(), publishDate: publishDate.value,
      bizInfo: bizInfo.value.trim(), imageData, imageName: file ? file.name : ''
    };
    const r = await fetch(GAS_URL, { method:'POST', body: JSON.stringify(body) });
    const j = await r.json();
    if (j.success) {
      document.getElementById('form').classList.add('hide');
      document.getElementById('done').classList.add('show');
    } else { alert('제출 실패: ' + (j.error || j.reason || '다시 시도해주세요')); btn.disabled=false; btn.textContent='검수 신청하기'; }
  } catch (err) { alert('네트워크 오류. 다시 시도해주세요.'); btn.disabled=false; btn.textContent='검수 신청하기'; }
});
</script></body></html>
```

- [ ] **Step 3: `시작 8080.bat`**

```bat
@echo off
chcp 65001
cd /d "%~dp0"
start http://localhost:8080
python -m http.server 8080
```

- [ ] **Step 4: 로컬 확인**

Run: `cd "프로젝트/for_Release/고방-광고상품" && python -m http.server 8080`
Expected: 브라우저에서 폼 렌더 — 상품 선택 토글·필수검증·파일첨부 동작 (GAS_URL 비어있어 제출만 미동작)

- [ ] **Step 5: 커밋**

```bash
git add index.html colors_and_type.css "시작 8080.bat" && git commit -m "feat: 광고 신청폼 index.html + 고방 디자인 토큰"
```

---

## Task 7: 노션 허브 페이지 + 토스 결제링크 상품 내용

**Files:** (코드 아님 — Notion MCP 사용)

- [ ] **Step 1: "고방 광고상품" 페이지(`6703826995438395956001d30bc52f09`)를 허브로 갱신**
  - 관련 URL 콜아웃(gray_bg): DB(시트 `18_Oq…`) · R(신청폼 `gobangmkt.github.io/gobang_ad_request/`) · 결제링크(토스 2종, 생성 후) · 솔라피 콘솔 · 가설(있으면 mention)
  - 액션플랜 토글(한 일/남은 일, 2026-06-12 기준)

- [ ] **Step 2: 하위 페이지 "토스 결제링크 상품 내용" 생성** — 빌리투어 `💳` 포맷으로 상품 2종:
  - ① 게시 1회 — 상품명 `고방 오픈카톡방 광고 (게시 1회)`, 금액 `55000`, 소개글(청년주거 커뮤니티 1,178명·[광고]표기·다음날 자정까지·사전검수), 교환/환불(게시 전 전액환불·게시 후 불가), 결제 알림톡 문구, 노출 ON
  - ② 게시+공지고정 — 상품명 `고방 오픈카톡방 광고 (게시+공지고정 24h)`, 금액 `110000`, 동일 골격 + 공지 고정 24h
  - SOLAPI 알림톡 템플릿 문구(결제링크 버튼 포함) 참고 섹션

- [ ] **Step 3: 사용자 확인** — 노션 페이지 검토 요청.

---

## Task 8: README + .gitignore + GitHub repo + 배포

**Files:**
- Create: `README.md`, `.gitignore`

- [ ] **Step 1: `.gitignore`**

```
gas-code/.clasp.json
node_modules/
.DS_Store
```

- [ ] **Step 2: `README.md`** — 개요·코어(신청→검수→알림톡 흐름)·기술스택·실행/배포(GitHub Pages URL·GAS·시트ID·시크릿 위치 값 미기재·로컬 8080)·배포링크(`https://gobangmkt.github.io/gobang_ad_request/`)·미래 어드민 흡수 메모.

- [ ] **Step 3: GitHub repo 생성 + push**

```bash
gh repo create gobangMkt/gobang_ad_request --public --source=. --remote=origin --push
```

- [ ] **Step 4: GitHub Pages 활성화** — repo Settings→Pages→main/(root). URL 확인.

- [ ] **Step 5: 커밋·푸시**

```bash
git add README.md .gitignore && git commit -m "docs: README + .gitignore" && git push
```

---

## 운영자(사용자) 선행/마무리 작업 — 코드와 별개

1. **토스 결제링크 2종 생성** (노션 정리본 기반, 55,000 / 110,000) → 링크를 `Code.gs`의 `PAY_LINK_BASIC/PIN` + 알림톡 템플릿 버튼에 반영.
2. **SOLAPI 카카오 알림톡 템플릿 2종 등록** (결제요청, 버튼에 토스링크) → `TEMPLATE_PAYMENT_BASIC/PIN` 채움.
3. **GAS Script Property 등록**: `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SOLAPI_PF_ID`.
4. **clasp 바인딩**: `gas-code/`에서 `clasp clone`(시트 컨테이너 스크립트) 또는 standalone+`SPREADSHEET_ID` openById 방식 → `clasp push` → `clasp deploy` → `/exec` URL을 `index.html`의 `GAS_URL`에 기입 후 재푸시.
5. **`setupSheets()` + `setupTrigger()` 1회 실행** (GAS 편집기).
6. **E2E 검증**: 폼 제출 → 시트 1행 + Drive 이미지 1개 → K열 '발송하기' → 알림톡 수신 확인.

---

## Self-Review

- **스펙 커버리지**: 상품 2종/금액✓(T5,T6,T7) · 신청폼 입력항목 8개✓(T6) · 이미지1장 Drive✓(T3) · GAS 2탭✓(T5) · onEdit 발송 로직✓(T4) · 토스+노션 허브✓(T7) · 어드민 흡수(설정탭 카탈로그)✓(T5) · 배포✓(T8). 빌리투어 대비 제거(지점검증·크롤링·작업내역)는 미구현으로 반영됨.
- **플레이스홀더**: `GAS_URL`·`PAY_LINK_*`·`TEMPLATE_*`는 의도적 공란(운영자 선행작업에서 채움) — 명시됨. 그 외 TODO 없음.
- **타입 일관성**: `productLabel` 코드→라벨('게시1회'/'게시+공지고정') T2·T4·T6 일치. 결제 발송열=K(11)/발송시간=L(12)/게시완료=M(13) T4·T5 일치. 상품코드 A/B 전 태스크 일치.
